# C

Use C for:

- Low-level systems experiments.
- Device-facing prototypes.
- Storage/runtime fundamentals.
- Memory and resource constraint learning.

Put headers in `include/` and implementation files in `src/`.
