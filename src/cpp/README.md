# C++

Use C++ for:

- Performance-oriented local runtime experiments.
- Systems prototypes.
- Interfaces that may need stronger structure than C.

Put headers in `include/` and implementation files in `src/`.
