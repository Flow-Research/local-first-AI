# Tests

Tests are organized by implementation language.

| Folder | Purpose |
|---|---|
| `python/` | Python unit and integration tests |
| `c/` | C build/run checks |
| `cpp/` | C++ build/run checks |
| `rust/` | Rust checks and tests |

Every weekly report should record what was tested or why tests were not added yet.
